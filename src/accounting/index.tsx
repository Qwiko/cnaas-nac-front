import ListAltIcon from "@mui/icons-material/ListAlt";
import { AccountingList, AccountingShow } from "./Accounting";

export default {
  list: AccountingList,
  show: AccountingShow,
  icon: ListAltIcon,
  // recordRepresentation: (record: Network) => `"${record.reference}"`,
};
